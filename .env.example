# ===============================================
# ML Court Order Assistant - Конфигурация окружения
# ===============================================
# Скопируйте этот файл в .env и заполните значения:
#   cp .env.example .env
#
# ВАЖНО: Никогда не коммитьте .env в систему контроля версий!
# ===============================================

# База данных
# Для разработки (SQLite):
DATABASE_URL=sqlite:///./dev.db
# Для продакшена (PostgreSQL):
# DATABASE_URL=postgresql://user:password@localhost:5432/ml_court_db

# JWT Секретный ключ
# Сгенерируйте безопасный ключ для продакшена:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=измените-на-безопасный-ключ-в-продакшене

# ===============================================
# Токен Telegram бота
# ===============================================
# Как получить токен:
# 1. Откройте Telegram и найдите @BotFather
# 2. Отправьте команду /newbot
# 3. Следуйте инструкциям для создания бота
# 4. Скопируйте токен (выглядит как: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz)
#
# Если токен не работает (ошибка 401 Unauthorized):
# 1. Перейдите в @BotFather
# 2. Отправьте /mybots и выберите вашего бота
# 3. Выберите "API Token" → "Revoke current token" для генерации нового
#    (или создайте нового бота командой /newbot)
# 4. Обновите этот файл новым токеном
#
# ВАЖНО: Если вы случайно закоммитили токен в git,
# Telegram автоматически отзовёт его из соображений безопасности!
# Вам нужно сгенерировать НОВЫЙ токен.
# ===============================================
TELEGRAM_BOT_TOKEN=ваш-токен-от-botfather
