# ml-court-order-assistant-0-
Система «ИИ-помощник по судебному приказу» автоматизирует полный цикл работы по взысканию бесспорной задолженности за ЖКУ от момента получения данных из бухгалтерии до передачи исполнительного документа в ФССП.

📌 Описание проекта

Этот репозиторий содержит объектную модель, разработанную в рамках домашнего задания №1 курса «Практическая ML-инженерия: MLOps и разработка проектов. 3 поток».

Модель описывает архитектуру сервиса, который включает:

* работу с пользователями и ролевой системой;
* управление балансом и транзакциями;
* взаимодействие с ML-моделями через очередь задач;
* юридический workflow от задолженности до исполнительного производства.

Цель задания — продемонстрировать умение:

* выделять сущности,
* проектировать их связи,
* применять ООП (инкапсуляция, наследование, полиморфизм),
* готовить доменную модель под будущее расширение (REST, ORM, RabbitMQ, Docker).


🏛️ Архитектурные сущности

👤 Пользователи и роли

* **User**
  Базовый класс с инкапсулированным балансом и паролем.
* **Manager**
  Может просматривать все дела, но не управляет чужими балансами.
* **Admin**
  Имеет доступ ко всем делам и может одобрять пополнения.

💰 Баланс и транзакции

* **Transaction**: история операций
* **TopUpRequest**: запросы пополнения, которые администратор может одобрять

🤖 ML-модели и задачи

* **MLModel** — протокол интерфейса модели
* **CourtOrderSuitabilityModel** — реализация конкретной модели
* **MLTask** — объект задачи в очереди (RabbitMQ + воркеры)

⚖️ Домен «Судебный приказ»

* **DebtCase** — дело должника
* **Calculation / CalculationItem** — расчёты задолженности
* **PretrialNotice** — досудебное уведомление
* **CourtOrder** — заявление и судебный приказ
* **Enforcement** — исполнительное производство (ФССП)


🧩 Принципы ООП, использованные в работе

🔒 Инкапсуляция

Поля `_password_hash` и `_balance_credits` скрыты от прямого изменения.
Работа с ними происходит через методы:

* `check_password()`
* `balance`
* `top_up()`
* `spend()`

Это защищает объект от некорректного использования.


🧬 Наследование

Роли построены на основе иерархии:

```
User
 ├── Manager
 └── Admin
```

Admin расширяет User дополнительными правами.
Manager расширяет User доступом к просмотру всех дел.


🌀 Полиморфизм

Методы:

* `can_view_all_cases()`
* `can_approve_topups()`

вызываются одинаково для любой роли,
но поведение зависит от конкретного класса.

Благодаря этому код, который работает с ролями,
не зависит от их конкретного типа.


📂 Структура репозитория

```
📦 ваш_репозиторий
 ├── domain_model.py   # объектная модель
 ├── README.md         # описание проекта (этот файл)
```


🔧 Техническая база для следующих ДЗ

Эта модель станет основой для:

* проектирования БД через ORM (SQLAlchemy / Django ORM),
* построения REST API (FastAPI),
* интеграции с очередями задач (RabbitMQ),
* разработки Telegram-бота,
* контейнеризации проекта (Docker),
* мониторинга и логирования системы.

🚀 Как запустить учебный проект дальше

На следующих этапах курса тебе предстоит:

1. Превратить object model → ORM model
2. Реализовать API и Telegram-бота
3. Подключить очередь RabbitMQ с распределёнными воркерами
4. Обернуть все сервисы в Docker Compose
5. Провести нагрузочные тесты и включить мониторинг

Этот репозиторий является **стартовой точкой архитектуры проекта**.


🔗 Файлы задания

* [`domain_model.py`](./domain_model.py)


🧱 Автор модели

Студент курса «Практическая ML-инженерия: MLOps и разработка проектов. 3 поток» Калинин Евгений Владимирович.

Проект создан в рамках ДЗ №1:
«Спроектировать объектную модель сервиса»
