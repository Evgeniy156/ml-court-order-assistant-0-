# 📱 Пошаговая инструкция по локальному тестированию Telegram бота

Эта инструкция поможет вам настроить и протестировать Telegram бота из ДЗ №4 на вашем локальном компьютере.

---

## 📋 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Шаг 1: Клонирование репозитория](#шаг-1-клонирование-репозитория)
3. [Шаг 2: Переключение на нужную ветку](#шаг-2-переключение-на-нужную-ветку)
4. [Шаг 3: Создание виртуального окружения](#шаг-3-создание-виртуального-окружения)
5. [Шаг 4: Установка зависимостей](#шаг-4-установка-зависимостей)
6. [Шаг 5: Получение токена Telegram бота](#шаг-5-получение-токена-telegram-бота)
7. [Шаг 6: Настройка переменных окружения](#шаг-6-настройка-переменных-окружения)
8. [Шаг 7: Инициализация базы данных](#шаг-7-инициализация-базы-данных)
9. [Шаг 8: Запуск Telegram бота](#шаг-8-запуск-telegram-бота)
10. [Шаг 9: Тестирование функционала бота](#шаг-9-тестирование-функционала-бота)
11. [Дополнительно: Запуск REST API](#дополнительно-запуск-rest-api)
12. [Решение проблем](#решение-проблем)

---

## 🔧 Предварительные требования

Убедитесь, что на вашем компьютере установлены:

- **Python 3.10+** (рекомендуется 3.11)
- **pip** (менеджер пакетов Python)
- **Git**
- **Telegram** (мобильное приложение или десктоп-клиент)

### Проверка установки Python:

```bash
# Windows
python --version

# macOS/Linux
python3 --version
```

Ожидаемый вывод: `Python 3.11.x` или выше.

---

## 📥 Шаг 1: Клонирование репозитория

Откройте терминал (Command Prompt, PowerShell или Terminal) и выполните:

```bash
# Клонируем репозиторий
git clone https://github.com/Evgeniy156/ml-court-order-assistant-0-.git

# Переходим в директорию проекта
cd ml-court-order-assistant-0-
```

---

## 🔀 Шаг 2: Переключение на нужную ветку

Переключитесь на ветку с кодом ДЗ №4:

```bash
git fetch origin
git checkout copilot/add-ml-service-and-dashboard
```

Проверьте, что вы на правильной ветке:

```bash
git branch
```

Вы должны увидеть `* copilot/add-ml-service-and-dashboard`.

---

## 🐍 Шаг 3: Создание виртуального окружения

### Windows:

```bash
# Создаём виртуальное окружение
python -m venv venv

# Активируем его
venv\Scripts\activate
```

### macOS/Linux:

```bash
# Создаём виртуальное окружение
python3 -m venv venv

# Активируем его
source venv/bin/activate
```

После активации вы увидите `(venv)` в начале командной строки.

---

## 📦 Шаг 4: Установка зависимостей

Установите все необходимые библиотеки:

```bash
# Переходим в директорию app
cd app

# Устанавливаем зависимости
pip install -r requirements.txt

# Возвращаемся в корень проекта
cd ..
```

Это установит:
- `fastapi` - веб-фреймворк для REST API
- `aiogram` - фреймворк для Telegram ботов
- `SQLAlchemy` - ORM для работы с БД
- `passlib` - хеширование паролей
- И другие зависимости

---

## 🤖 Шаг 5: Получение токена Telegram бота

Для работы бота вам нужен токен от Telegram. Следуйте инструкции:

### 5.1. Откройте Telegram

Найдите бота **@BotFather** (официальный бот Telegram для создания ботов).

### 5.2. Создайте нового бота

1. Отправьте команду `/newbot`
2. Введите **имя бота** (это отображаемое имя, например: `ML Court Order Assistant Test`)
3. Введите **username бота** (должен заканчиваться на `bot`, например: `ml_court_test_bot`)

### 5.3. Получите токен

После создания бота @BotFather отправит вам сообщение с токеном вида:

```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

⚠️ **Важно:** Храните токен в секрете! Не публикуйте его в публичных репозиториях.

### 5.4. Скопируйте токен

Скопируйте полученный токен — он понадобится на следующем шаге.

---

## ⚙️ Шаг 6: Настройка переменных окружения

### Вариант A: Создание файла .env (рекомендуется)

Создайте файл `.env` в папке `app`:

```bash
# Находясь в корне проекта
cp app/.env.example app/.env
```

Откройте файл `app/.env` в текстовом редакторе и отредактируйте:

```env
# Database
DATABASE_URL=sqlite:///./dev.db

# JWT Secret (можно оставить как есть для тестирования)
SECRET_KEY=your-secret-key-change-in-production

# Telegram Bot Token - ВСТАВЬТЕ ВАШ ТОКЕН СЮДА
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

### Вариант B: Установка через терминал (альтернатива)

#### Windows (PowerShell):

```powershell
$env:DATABASE_URL = "sqlite:///./dev.db"
$env:SECRET_KEY = "test-secret-key"
$env:TELEGRAM_BOT_TOKEN = "ВАШ_ТОКЕН_ЗДЕСЬ"
```

#### Windows (Command Prompt):

```cmd
set DATABASE_URL=sqlite:///./dev.db
set SECRET_KEY=test-secret-key
set TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_ЗДЕСЬ
```

#### macOS/Linux:

```bash
export DATABASE_URL="sqlite:///./dev.db"
export SECRET_KEY="test-secret-key"
export TELEGRAM_BOT_TOKEN="ВАШ_ТОКЕН_ЗДЕСЬ"
```

---

## 🗄️ Шаг 7: Инициализация базы данных

Инициализируйте базу данных с демо-данными:

```bash
# Находясь в корне проекта
python -m storage.init_db
```

Ожидаемый вывод:

```
USING DATABASE_URL: 'sqlite:///./dev.db'
DB initialized successfully
Admin id=1, email=admin@example.com
User id=2, email=user@example.com
```

Это создаст:
- SQLite базу данных `dev.db`
- Демо-пользователя: `user@example.com` / `user123`
- Демо-администратора: `admin@example.com` / `admin123`
- Базовые ML модели

---

## 🚀 Шаг 8: Запуск Telegram бота

### 8.1. Установите переменные окружения (если используете Вариант B)

Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен.

### 8.2. Запустите бота

```bash
# Находясь в корне проекта
python -m app.src.telegram_bot
```

При успешном запуске вы увидите:

```
INFO:aiogram.dispatcher:Run polling for bot @your_bot_name in mode (Polling)
INFO:app.src.telegram_bot:Starting bot...
```

⚠️ **Бот будет работать, пока вы не остановите его** (нажмите `Ctrl+C` для остановки).

---

## 🧪 Шаг 9: Тестирование функционала бота

### 9.1. Найдите вашего бота в Telegram

1. Откройте Telegram
2. Найдите бота по username, который вы указали при создании (например, `@ml_court_test_bot`)
3. Нажмите **Start** или отправьте `/start`

### 9.2. Основные команды для тестирования

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню с кнопками |
| `/help` | Справка по командам |
| `/register` | Регистрация нового пользователя |
| `/login` | Вход в аккаунт |
| `/balance` | Проверка баланса |
| `/deposit` | Пополнение баланса |
| `/predict` | ML-предсказание |
| `/history` | История транзакций |
| `/logout` | Выход из аккаунта |

### 9.3. Сценарий полного тестирования

#### A. Регистрация нового пользователя:

1. Отправьте `/start`
2. Нажмите кнопку **📝 Регистрация**
3. Введите email: `test@example.com`
4. Введите пароль: `test1234`
5. ✅ Получите подтверждение успешной регистрации

#### B. Пополнение баланса:

1. Нажмите кнопку **➕ Пополнить**
2. Введите сумму: `100`
3. ✅ Получите подтверждение пополнения

#### C. Проверка баланса:

1. Нажмите кнопку **💰 Баланс**
2. ✅ Увидите текущий баланс: `100.00 кредитов`

#### D. Выполнение ML-предсказания:

1. Нажмите кнопку **🔮 Предсказание**
2. Введите сумму задолженности: `50000`
3. Введите сумму пени: `5000`
4. Введите дней просрочки: `120`
5. Введите долю оплаченного: `0.3`
6. Нажмите **Да** (физическое лицо)
7. ✅ Получите результат предсказания и списание кредитов

#### E. Просмотр истории:

1. Нажмите кнопку **📜 История**
2. ✅ Увидите список последних транзакций

#### F. Выход:

1. Нажмите кнопку **🚪 Выйти**
2. ✅ Вы вышли из аккаунта

### 9.4. Тестирование с демо-пользователем

Вместо регистрации вы можете войти под демо-пользователем:

1. `/start` → **🔑 Войти**
2. Email: `user@example.com`
3. Пароль: `user123`

Демо-пользователь уже имеет 100 кредитов на балансе.

---

## 🌐 Дополнительно: Запуск REST API

Параллельно с ботом вы можете запустить REST API:

```bash
# В новом терминале (с активированным venv)
cd ml-court-order-assistant-0-
uvicorn app.src.main:app --host 0.0.0.0 --port 8000
```

После запуска:
- **Swagger UI документация:** http://localhost:8000/docs
- **Главная страница:** http://localhost:8000/

---

## ❓ Решение проблем

### Проблема: `TELEGRAM_BOT_TOKEN не установлен!`

**Решение:** Убедитесь, что вы установили токен в переменных окружения или в файле `.env`.

```bash
# Проверьте, установлен ли токен
# Windows PowerShell:
echo $env:TELEGRAM_BOT_TOKEN

# macOS/Linux:
echo $TELEGRAM_BOT_TOKEN
```

### Проблема: `ModuleNotFoundError: No module named 'aiogram'`

**Решение:** Убедитесь, что вы активировали виртуальное окружение и установили зависимости.

```bash
# Активируйте venv
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r app/requirements.txt
```

### Проблема: `ModuleNotFoundError: No module named 'storage'`

**Решение:** Запускайте бота из корневой директории проекта:

```bash
cd ml-court-order-assistant-0-
python -m app.src.telegram_bot
```

### Проблема: `Billing account for user X not found`

**Решение:** База данных не инициализирована. Запустите:

```bash
python -m storage.init_db
```

### Проблема: Бот не отвечает в Telegram

**Причины:**
1. Бот не запущен (проверьте терминал)
2. Неверный токен
3. Проблемы с интернет-соединением

**Решение:** Перезапустите бота и проверьте логи в терминале.

### Проблема: `sqlite3.OperationalError: unable to open database file`

**Решение:** Убедитесь, что у вас есть права на запись в директорию проекта.

---

## 📝 Итоги

После выполнения всех шагов у вас будет:

✅ Работающий Telegram бот с функционалом:
- Регистрация и авторизация пользователей
- Просмотр и пополнение баланса
- ML-предсказания с оплатой кредитами
- История транзакций

✅ REST API на FastAPI с документацией

✅ SQLite база данных с демо-данными

---

## 📚 Дополнительные ресурсы

- [Документация aiogram 3.x](https://docs.aiogram.dev/en/latest/)
- [Документация FastAPI](https://fastapi.tiangolo.com/)
- [BotFather в Telegram](https://t.me/botfather)

---

*Автор: Создано для ДЗ №4 курса «Практическая ML-инженерия»*
