# 🚀 Быстрый старт ДЗ№5

### 1️⃣ Запуск системы (Docker)

```bash
# Клонируйте и запустите
git clone <repo-url>
cd ml-court-order-assistant-0-
docker-compose up -d

# Проверьте что все запущено
docker-compose ps
```

Должны работать 7 контейнеров:
- ✅ app (FastAPI)
- ✅ ml-worker-1, ml-worker-2, ml-worker-3 (воркеры)
- ✅ rabbitmq
- ✅ database
- ✅ web-proxy

### 2️⃣ Автоматический тест

```bash
# Установите Python зависимости (для тестового скрипта)
pip install requests

# Запустите автоматический тест
python test_api_manual.py
```

**Что делает тест:**
1. Регистрирует пользователя
2. Пополняет баланс
3. Отправляет задачу в очередь
4. Ждет обработки воркером
5. Показывает результат

### 3️⃣ Проверка RabbitMQ UI

Откройте: http://localhost:15672
- Логин: `guest`
- Пароль: `guest`

**Проверьте:**
- Queues → `ml_tasks` → **Consumers: 3** ✅
- Connections → минимум 4 подключения ✅

### 4️⃣ Ручной тест через API

```bash
# 1. Регистрация
curl -X POST http://localhost/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"test123"}'

# 2. Логин (сохраните токен)
curl -X POST http://localhost/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"test123"}'

# 3. Пополнение (используйте токен)
curl -X POST http://localhost/balance/deposit \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"amount":100}'

# 4. Предсказание (получите task_id)
curl -X POST http://localhost/predict \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "total_debt":50000,
    "penalty_amount":5000,
    "days_overdue":100,
    "payments_ratio":0.3,
    "is_physical_person":true
  }'

# 5. Статус задачи (используйте task_id из п.4)
curl http://localhost/task/1 \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 5️⃣ Проверка логов воркеров

```bash
# Посмотрите логи обработки
docker-compose logs ml-worker-1 | tail -20
docker-compose logs ml-worker-2 | tail -20
docker-compose logs ml-worker-3 | tail -20
```

**Ожидаемое в логах:**
```
[worker-1] Получена задача 1 из очереди
[worker-1] Задача 1 успешно обработана
```

### 6️⃣ Тест распределения нагрузки

```bash
# Отправьте 10 задач
for i in {1..10}; do
  curl -s -X POST http://localhost/predict \
    -H "Authorization: Bearer YOUR_TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"total_debt":50000,"penalty_amount":5000,"days_overdue":100,"payments_ratio":0.3,"is_physical_person":true}' \
    | jq '.task_id'
done

# Проверьте что задачи распределились по воркерам
docker-compose logs ml-worker-1 | grep "успешно обработана" | wc -l
docker-compose logs ml-worker-2 | grep "успешно обработана" | wc -l
docker-compose logs ml-worker-3 | grep "успешно обработана" | wc -l
```

---

## ✅ Критерии оценки

### Обязательные требования (10 баллов):

1. **RabbitMQ интеграция** ✅
   - [x] Сообщения отправляются в очередь
   - [x] Воркеры получают и обрабатывают задачи
   - [x] Режим "один издатель - несколько слушателей"

2. **ML Workers** ✅
   - [x] Несколько воркеров подключены (3 штуки)
   - [x] Валидация входных данных
   - [x] Выполнение предсказания
   - [x] Сохранение результата в БД

3. **API/Bot интеграция** ✅
   - [x] REST API отправляет задачи в очередь
   - [x] Telegram bot отправляет задачи в очередь
   - [x] Возможность проверить статус задачи

4. **Тестирование** ✅
   - [x] Работает через REST API
   - [x] Работает через Telegram Bot
   - [x] Автоматические тесты
   - [x] Документация

---

## 📚 Дополнительная информация

- **Полное руководство:** [TESTING_GUIDE.md](TESTING_GUIDE.md)
- **Архитектура:** [README.md](README.md)
- **API документация:** http://localhost:8000/docs

---

## 🐛 Проблемы?

### Сервисы не запускаются
```bash
# Пересоздайте контейнеры
docker-compose down -v
docker-compose up -d --build
```

### Воркеры не обрабатывают задачи
```bash
# Перезапустите воркеры
docker-compose restart ml-worker-1 ml-worker-2 ml-worker-3
```

### База данных не инициализирована
```bash
# Войдите в контейнер app и создайте таблицы
docker-compose exec app python -c "from storage.db import Base, engine; Base.metadata.create_all(bind=engine)"
```

---

## 📧 Контакты

При возникновении вопросов см. [TESTING_GUIDE.md](TESTING_GUIDE.md) или изучите логи:

```bash
# Все логи
docker-compose logs

# Конкретный сервис
docker-compose logs -f ml-worker-1
```

**🎉 Удачи при проверке!**
