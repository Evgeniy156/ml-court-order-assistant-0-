# === Telegram ===
TELEGRAM_BOT_USERNAME=@AI_Court_Order_Bot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
SECRET_KEY=change-me-secret-key

# === База данных (для Docker) ===
# Для запуска в Docker используйте имя сервиса 'database'
DATABASE_URL=postgresql://postgres:postgres@database:5432/ml_court

# === RabbitMQ (для Docker) ===
# Для запуска в Docker используйте имя сервиса 'rabbitmq'
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_QUEUE=ml_tasks

# === Прочие настройки ===
ENV=prod

